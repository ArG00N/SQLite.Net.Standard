This is a .NET console App. 
It references the SQLite.Net.Standard NuGet package.
To make changes in the core library,  drop the NuGet reference and add a reference to the SQLite.Net.Standard directly